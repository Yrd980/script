Puffer Fish > Text Expansions for Fish Typing consecutive dots after will automatically expand to , then and so on. It expands in place as you type to allow directory tab-completion to work. This can be useful with any command where you want to reference a parent directory that is more than one back. i.e. or . !dot dot demo It's a little hard to see in the demo because it expands as you type, but is being typed. -------------------------------------------------------------------------------- Typing will expand to the previous command, similar to Zsh, Bash, and other shells. For example, if you type you will likely see a permisison error. You can then type and it will expand to . !exclamation demo This can be especially useful if you don't want to retype a long command, or navigate around with the arrow keys. Or if you want to re-run a command as part of pipeline. -------------------------------------------------------------------------------- Typing will expand to the last argument. For example, if you searched packages with pacman and found nothing, you'd like to also search in the AUR. Installation Install with Fisher: Manually Just copy and to your directory. Make sure to create them if they don't already exist. LICENSE MIT